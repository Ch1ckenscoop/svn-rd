<!--
#summary Controls shotgun # of pellets.
#labels D-Cvar
-->
======Controls shotgun # of pellets.======

= Description =

Sets the number of shotgun pellets. Due to the client relying on the same code that was changed server-side, the client tracer effects will always show 7 pellets.

<!--
|| *Revision Added* || *Default* || *[CvarFlags Flags]* || *[ParamType Parameter Type]* ||
|| r94 || `7` || [FCVAR_CHEAT] || [Int] ||
-->
{|
|| Revision Added || Default || [[CvarFlags.wiki | CvarFlags]] || [ParamType Parameter Type] 
|-
|| r94 || `7` || [[FCVAR_CHEAT.wiki|FCVAR_CHEAT]] || [Int] 
|}